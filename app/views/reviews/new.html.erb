<div class="container">
  <h1>You're writing a review for <%= @restaurant.name %></h1>

  <%= simple_form_for [@restaurant, @review] do |form| %>
    <%= form.input :content %>
    <%= form.input :rating %>
    <%= form.submit %>
  <% end %>
</div>
